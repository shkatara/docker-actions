name: Render Helm Template

on:
    push:
        branches:
            - helm
    pull_request:
        branches:
            - main
    workflow_dispatch:

jobs:
    render:
        runs-on: ubuntu-latest

        steps:
        - name: Checkout code
          uses: actions/checkout@v2

        #- name: Install Helm
        #  run: |
        #        curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

        - name: Render Helm template
          run: |
                helm template .

        # - name: Commit and push rendered template to gitops branch
        #   run: |
        #     git config --global user.name "github-actions"
        #     git config --global user.email "github-actions@github.com"
        #     git checkout --orphan gitops || git checkout gitops
        #     helm template . > rendered-template.yaml
        #     git add rendered-template.yaml
        #     git commit -m "Add rendered Helm template"
        #     git push origin gitops --force